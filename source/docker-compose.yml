services:
  web:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      SECRET_KEY: ${SECRET_KEY:-defaultsecret}
      DATABASE_URL: ${DATABASE_URL:-sqlite:///aurum/instance/app.db}
    volumes:
      - ./instance:/app/instance
