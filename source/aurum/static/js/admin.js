// admin.js for Flask version
// This file ONLY handles optional dynamic behavior.
// It does NOT check admin access (Flask already handles that)
// It does NOT load sales from localStorage anymore.

document.addEventListener("DOMContentLoaded", () => {
  console.log("Admin dashboard loaded.");
});
